# program to check Armstrong number in a certian interval

lower =100
upper=2000

for num in range(lower,upper+ 1):
    # order of number
    order = len(str(num))

    #inititalize sum
    sum=0

    temp=  num
    while temp >0:
        digits =temp % 10
        sum +=digits ** order
        tem
          print(num)
        
